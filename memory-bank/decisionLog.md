# Decision Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-01-15 | Fixed Windows Unicode Encoding in hub_manager.py. | Windows cp1252 encoding crashed when colorama/logging attempted to render emoji characters (🔑, ⚡, ✅, ❌, ⚠️). Replaced all emojis with ASCII-safe alternatives: `[KEY]`, `[CACHE HIT]`, `[SUCCESS]`, `[FAILED]`, `[FALLBACK]`. This prevents encoding crashes on Windows while maintaining log readability. |
| 2026-01-15 | Mandated PowerShell Start-Job for LangGraph Server on Windows. | VS Code integrated terminals and direct `python -m langgraph_cli dev` execution caused premature server exit after initialization. The server would start, load all graphs (~50s), then immediately shut down. Using `Start-Job` provides proper process isolation and prevents terminal interference. |
| 2026-01-15 | Added `--no-reload` flag to LangGraph CLI. | File watcher restarts were causing unnecessary server restarts during development. The `--no-reload` flag prevents this and reduces startup overhead. |
| 2026-01-15 | Documented 50-65 second server startup time as expected. | The `google.api_core._python_version_support.check_python_version()` function scans all installed packages via `packages_distributions()`, taking ~26-30 seconds. Combined with agent initialization (~20s), total startup is ~50-65s. This is unavoidable but cached after first import. |
| 2026-01-15 | Prioritized Google Studio Voices in System Config. | To align with the project's Google-centric architecture and reduce reliance on third-party Replicate proxies (Minimax/XTTS), we explicitly prioritized `en-US-Studio` models in the configuration. This ensures the Composer Agent attempts to use high-quality native Google TTS first. |
| 2026-01-15 | Synced Local Configuration to LangSmith Hub (Overwrite). | The remote `deepagents-system-config` on LangSmith was outdated (pointing to Anthropic Claude Haiku) and overriding the local `Gemini-2.0-Flash` defaults. To enforce consistent behavior and use the correct SOTA model, we pushed the local `DEFAULT_SYSTEM_CONFIG` to the Hub, establishing it as the single Source of Truth. |
| 2026-01-15 | Enforced "Fail Fast" Runtime Policy. | To solve the issue of Agents spinning in 15-step retry loops or hallucinating after tool execution ("Echo Chambers"), we hard-removed all `while` loops and retry logic. Agents must now succeed on the first attempt or crash immediately, making root cause analysis trivial. |
| 2026-01-15 | Adopted Linear Chain for Composer Agent. | The standard ReAct pattern was causing the Composer to "think too much" after generating music, leading to hallucinated text follow-ups. We replaced it with a strict Linear Chain (`Reason -> Tool -> Stop`) that returns the tool output directly as the final answer. |
| 2026-01-14 | Fixed Cinematographer Case Sensitivity. | The system config was sending 'replicate' (lowercase) but the code checked for 'Replicate' (Title Case), causing video generation to fail. Made the check case-insensitive. |
| 2026-01-14 | Hardened Director Persona against 'Meta-Review' Hallucinations. | The Director Agent was outputting summaries of the plan instead of the plan itself. Updated `prompts.py` with strict 'YOU ARE THE AUTHOR' instructions to force actionable script output. |
| 2026-01-14 | Enforced 'Pure Planner' Mode for Director. | Removed all tool access from the Director Agent to prevent `GraphRecursionError` caused by infinite validation loops. The Director MUST produce text-only plans that are routed by the Graph, not executed via internal tools. |
| 2026-01-14 | Enforced Robust Content Parsing for Production Agents. | `Composer` and `Cinematographer` now explicitly inspect output types (String vs List[Block]) before processing. This handles the variance in LangChain/Anthropic output formats that was causing `TypeError` crashes during successful generations. |
| 2026-01-14 | Removed Researcher Agent from Orchestration Graph. | To simplify the pipeline and resolve stability issues, we removed the Researcher Node from the hot path. The Director now routes directly to Validation (or Production) without the intermediate verification step. |
| 2026-01-14 | Implemented "Zero Touch" Prompt Architecture. | Removed all hardcoded prompts and schemas (Composer, Director, etc.) from `agent.py` files. All prompts are now managed via `hub_manager.py` using a "Pull or Push" self-healing pattern. This centralizes prompt versioning in LangSmith Hub while ensuring local execution reliability. |
| 2026-01-14 | Enforced 'Cardinal Context Rule' for Copilot Workflow. | To prevent regressions and logic errors caused by blind edits, Copilot must now strictly read the entire file (lines 1-1000+) before acting on it. This ensures all imports, class structures, and subtle logic are understood before modification. |
| 2026-01-14 | Implemented 'agency_graph.py' via LangGraph StateGraph. | Moved orchestration logic out of the Director Agent (Tool Calling) and into a formal StateGraph. This provides visibility, state persistence, and clear routing logic (Researcher -> Validator -> Production), adhering to the "Gold Standard" of agent orchestration. |
| 2026-01-14 | Implemented 'Zero Touch' Graph Configuration. | Added `GraphConfig(TypedDict)` to the StateGraph. This allows runtime behavior (Parallelism, Validation Requirements, Model Providers) to be toggled via LangSmith configuration without code deployment. |
| 2026-01-14 | Prioritize Cloud URLs for HITL Asset Identification. | Local file paths (`C:\Users...`) are inaccessible in remote observability tools like LangSmith. To ensure the Human-in-the-Loop review process is executable from the Cloud dashboard, agents must yield the GCS URL (`https://...`) as the unique identifier for assets requiring approval. |
| 2026-01-14 | Implement Blocking 'Approval Gate' for Media Generation. | Autonomous asset generation risks quota waste and quality drift. We implemented a mandatory `HITL_REVIEW_REQUIRED` signal that halts execution until the specific asset ID is present in `approved_assets.json`, giving the user final cut authority. |
| 2026-01-13 | Reverted to Minimax Music-1.5 from ACE-Step. | While ACE-Step offers control, Minimax 1.5 provides superior "Radio Quality" audio consistency. The character limit (600) is a known constraint we will mitigate via "High Density" prompting rather than technical hacks. |
| 2026-01-13 | Adopted "High Density/Compressed Imagery" Prompt Strategy. | To solve the Minimax 600-char limit, we shifted from "Truncation" (cutting off lyrics) to "Compression" (writing fewer, punchier lines) to ensure a full song arc fits within the API budget. |
| 2026-01-11 | Adopted XTTS-v2 (via Replicate) as the primary Voice Engine. | The previously used Minimax API was unavailable/unreliable, and Bark was too slow for real-time interaction. XTTS-v2 offers a good balance of quality and latency, supported by local reference audio injection. |
| 2026-01-11 | Renamed application entry point from DeepAgents.py to orchestrator.py. | The file name 'DeepAgents.py' collided with the installed middleware package 'deepagents', causing circular import errors and namespace confusion. |
| 2026-01-11 | Standardized on OpenTelemetry (OTLP) for Observability. | To ensure consistent tracing across LangSmith and other tools, we enabled OTLP (LANGSMITH_OTEL_ENABLED=true) targeting localhost:4318, providing a vendor-neutral observability path. |
| 2026-01-11 | Enforced MemoriPilot Protocol for Copilot Context. | To solve the issue of Copilot losing context between turns/sessions, we established the Memory Bank (MemoriPilot) as the mandatory 'long-term memory' that must be read/updated every session. |
| 2026-01-11 | Implemented Ontology-Driven Architecture (2026-01-05). | To ensure consistent agent behavior, we moved from pure prompt engineering to JSON-based Ontologies that define concepts and scoring rules. This allows programmatically checking agent outputs against a 'truth' definition. |
| 2026-01-11 | Established Reinforcement Feedback Loop (2026-01-05). | Commercial Agents now use a `bad_examples.md` file. The Confidence Agent rejects poor outputs (Score < 7) and appends them to this file, which is then fed back into the Research Agent's context to prevent repeat mistakes. |
| 2026-01-11 | Standardized Code Quality via Strict Pylinting (2026-01-08). | To ensure maintainability, we enforced a strict 10/10 Pylint standard across all core modules (`agent_brain`, `Copilot`, `model_registry`). |
| 2026-01-11 | Merged Copilot_Ontology.md into .github/copilot-instructions.md. | To consolidate the system's 'Constitution', we merged the Ontology Canon into the primary instructions file. This ensures that every session initialization loads both the MemoriPilot protocol and the architectural/behavioral ontology. |
| 2026-01-11 | Migrate from Replicate-centric to Google Vertex AI-centric Architecture | To address cost concerns and leverage higher-tier models for complex reasoning. Identified that standard Vertex AI models (Gemini-3-Pro-Preview, Lyria) have restrictive 2 RPM quotas suitable only for testing, not production. Selected 'Gemini 2.0 Flash' and 'Imagen 4 Fast' as the 'Winning Stack' to bypass these limits while providing SOTA speed and quality. Replicate serves as a necessary fallback for Audio/Video due to Google's strict quota blocks on those modalities. |
| 2026-01-11 | Disable 'deepagents' Middleware (v0.3.1) in favor of Native LangGraph | The 'deepagents' middleware library (v0.3.1) generated Pydantic validation errors ('Input should be a valid dictionary') when wrapping modern Gemini objects. Disabling the middleware and reverting to `langgraph.prebuilt.create_react_agent` eliminated these errors and restored system stability, while preserving the ability to bind tools natively. |
| 2026-01-11 | Adopt ChatGoogleGenerativeAI over ChatVertexAI | ChatVertexAI is legacy/foundational and lacks seamless support for some modern functional calling patterns required by LangGraph's tool binding. Moving to ChatGoogleGenerativeAI (via `langchain-google-genai`) ensures compatibility with the latest Vertex AI features and follows current LangChain best practices. |
| 2026-01-11 | Confirmed System Health but Identified Critical Quota Outage. | Multiple verification runs confirmed that the Agent Factory, Database Connections, and Middleware are functional. However, a hard '429 RESOURCE_EXHAUSTED' error persists across all accessible Gemini models (Flash, Pro, Lite) in the current Google Cloud Project context. This confirms the code is correct but the API usage limits have been hit. We will pause active development requiring LLM inference until quotas reset. |
| 2026-01-11 | Switching Intelligence Provider from Google to Anthropic/OpenAI (Due to Quota Friction). | The user is frustrated with Google Cloud's convoluted quota system and hard '429' blocks despite having credits. They are willing to incur cost for reliability and ease of use. Anthropic (Claude) and OpenAI (GPT-4o) are premier choices with native LangChain/LangSmith support. We will pivot the entire architecture away from the Google-specific 'Vertex AI' implementation to a standard 'ChatAnthropic' or 'ChatOpenAI' path to unlock progress. |
| 2026-01-11 | Enforce 'No-Failover' Policy for Prompts and Providers. | System must crash rather than using dummy agents or local prompt fallbacks to ensure epistemological integrity. Defaulting Intelligence to Anthropic and Media to Replicate aligns with best-in-class capabilities. |
| 2026-01-11 | Adopt Native LangSmith SDK 'workspace_id' Injection for Organization Keys. | Relying on `os.environ["LANGSMITH_WORKSPACE_ID"]` proved unreliable in the complex `ignite_atlas` runtime due to inconsistent variable persistence/import timing. We now pass `workspace_id` explicitly to the `Client()` constructor, ensuring that Organization-Scoped API Keys always have the required authorization context, preventing 400/403 errors. |
| 2026-01-12 | Switched Composer Agent to Direct Tool Execution (No Recursion). | The Composer Agent's `compose_tool` was calling `create_composer_agent` recursively, leading to infinite loops and crashes. We replaced this with `generate_music_tool` which directly calls the `minimax/music-01` (or fallback) API. This simplifies the graph and ensures reliable execution. |
| 2026-01-12 | Upgraded Composer Agent to Minimax Music-1.5. | To resolve persistent "audio format not supported" errors caused by Minimax Music-01 rejecting Lyria-generated WAV files (and lacking local FFmpeg for conversion), we switched to Minimax Music-1.5. This model generates full songs (music + vocals) from text prompts without requiring pre-generated instrumental tracks or strict file format dependencies, simplifying the pipeline and removing the failure point. |
| 2026-01-12 | Migrated Persistence to `psycopg` (v3) Binary. | The adoption of `AsyncPostgresSaver` in `DeepAgents/persistence.py` necessitated a move from `psycopg2` (v2) to `psycopg` (v3). On Windows, this required explicitly installing `psycopg-binary` and configuring the `asyncio` event loop policy to prevent `ImportError: libpq` and `NotImplementedError` crashes. |
| 2026-01-12 | Standardized Evaluation Retries with `tenacity`. | To handle API rate limits robustly during evaluation, we replaced hardcoded `time.sleep()` with `tenacity`'s exponential backoff decorators. This improves test reliability and adheres to best practices. |
| 2026-01-12 | Suppressed Pylint Import Errors via `.pylintrc`. | Due to the complex local module structure and VS Code environment, `import-error` warnings were creating excessive noise. We created a `.pylintrc` to globally disable this specific check, allowing us to focus on logic and syntax errors. |
| 2026-01-12 | Implemented Reflexion Loop for Composer Agent Logic. | To resolve user concerns about 'brittle' truncation hacks, we replaced the simple lyric truncation with a robust **Reflexion Loop**. The Composer Agent now iteratively validates generated lyrics against the Minimax Music-1.5 API schema (10-600 characters). If constraints are violated, it feeds the error back to the LLM for a rewrite (up to 3 attempts) before falling back to truncation, ensuring the system 'manipulates the model' to adhere to specs rather than just modifying the output. |
| 2026-01-12 | Enshrined Minimax Schema in System Prompt (Dynamic Prompting). | To eliminate the need for retries and truncation, we moved the Minimax Music-1.5 schema rules (Verse/Chorus/Bridge/Outro budgets) directly into the Composer's prompt. We also added a 'Transparency Patch' that forces the final output to display the *actual* lyrics sent to the API, preventing the Agent from hallucinating a longer version than what was generated. |
| 2026-01-12 | Standardized Replicate URL Extraction (List vs String). | Replicate SDK returns inconsistent output types (single URL string vs list of strings) depending on the model version (e.g., Minimax Music-1.5 returning lists). This caused 'E006' failures when the downstream handler expected a string. We implemented `_extract_replicate_url` as a universal helper to safely parse both formats, preventing silent failures and subsequent hallucinations. |
| 2026-01-13 | Integrated ACE-Step (lucataco/ace-step) as Default Music Model. | To address user demand for controllable, high-quality music generation, we integrated `lucataco/ace-step` into the Composer Agent. We implemented specialized Prompting schemas (TAGS/LYRICS/VERSE/CHORUS), hardcoded advanced generation parameters (Euler/APG/Scale 15) matching model documentation, and added duration parsing logic. Legacy models (Minimax/Lyria) are preserved as robust fallbacks. |
| 2026-01-13 | Shifted to Configuration-Driven Architecture (LangSmith). | To eliminate hardcoded GUI selectors and manual updates for new models, we implemented `SystemConfiguration` (`DeepAgents/system_config.py`). The system now pulls its capabilities matrix and agent defaults directly from a LangSmith Prompt (`deepagents-system-config`), allowing runtime updates without code changes. |
| 2026-01-13 | Consolidated Assets into Unified `Artifacts/` Structure. | To clean up the repo and simplify path management, we deprecated scattered `data/` and `recovered_assets/` folders. All generated content now lives in a canonical `Artifacts/` directory (Audio, Video, Data, Recovered), improving organization and backup logic. |
| 2026-01-13 | Removed Explicit GUI Model Selectors. | User Policy decision: The end-user should not manually select LLMs. The system (via LangSmith Config) determines the optimal model based on the request ("Overloaded Function" pattern). The GUI now displays the Active Config Matrix instead of dropdown controls. |
| 2026-01-13 | Revert OTLP Enforcement to Standard LangChain Tracing | User reported `ConnectionRefusedError` on localhost:4318 and confirmed they have a direct connection. The 'Gold Standard' for LangSmith is `LANGCHAIN_TRACING_V2=true` which uses the standard HTTP client to the Cloud API, removing the need for a local collector unless explicitly desired. This fixes the crash. |
| 2026-01-14 | Disabled HITL Review Gates for Asset Generation. | User feedback indicated that the `HITL_REVIEW_REQUIRED` blocking mechanism was interfering with the development flow. Temporarily commented out the blocking logic in Composer and Cinematographer agents to allow unhindered asset generation testing. |
| 2026-01-14 | Enforced 'Jewel Standard' Code Quality (10/10). | To improve long-term maintainability and reduce subtle bugs, we mandated that all core orchestration files (beginning with `agency_graph.py`) must achieve a perfect Pylint score and zero Pylance errors. This required refactoring logging, line lengths, and type hints to be strict yet compliant. |
| 2026-01-16 | Implemented Command-Based Mesh Routing Architecture. | The Director Agent was not delegating to production agents. Rather than hardcoding keyword-based routing (LOW MATURITY), we implemented LangGraph's Command pattern where all nodes return `Command[Literal[...]]`. Agents use delegation tools that return `HANDOFF:agent:directive` strings, which are parsed to determine dynamic routing. This eliminates conditional edges entirely and creates a true mesh network. |
| 2026-01-16 | Created Agent-Specific Delegation Tool Sets. | Added `DIRECTOR_TOOLS`, `RESEARCHER_TOOLS`, `CONFIDENCE_TOOLS`, `COMPOSER_TOOLS`, `CINEMATOGRAPHER_TOOLS`, `EDITOR_TOOLS` to `inter_agent_comms.py`. Each set contains `discover_agents` plus relevant `delegate_to_*` functions, enabling curated inter-agent communication based on role. |
